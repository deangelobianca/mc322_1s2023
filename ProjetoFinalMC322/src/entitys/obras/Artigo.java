package src.entitys.obras;

public class Artigo extends Obra {
    public Artigo(String titulo, String autor, boolean isReservado) {
        super(titulo, autor, isReservado);
    }

    @Override
    public String toString() {
        return super.toString();
    }
}
